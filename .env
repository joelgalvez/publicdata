# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The first time someone visits the site there is no filter options stored in the localStorage,
# Then it will redirect to this url below instead
NEXT_PUBLIC_DEFAULT_URL="/q?city-Amsterdam=1&city-Online=1&tag-Art=1&tag-New Music Now=1&tag-Academia=1&tag-Internet related=1&tag-Other=1"

# This is the sources URL JSON, it will be appended with ?random=(random number) for cache busting
NEXT_PUBLIC_SOURCES="http://publicdata.jgdev.xyz/export/"

# Pinned tags, shown above "see all"
NEXT_PUBLIC_PINNED_TAGS="Art,New Music Now,Academia,Internet related,Other"

